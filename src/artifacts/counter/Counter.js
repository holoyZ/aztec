/** Placeholder — replaced by `npm run compile:counter` (see src/artifacts/counter/README.md, issue #8). */
export class CounterContract {
  static deploy(..._args: unknown[]) {
    throw new Error("Run `npm run compile:counter` to generate contract bindings.");
  }

  static at(..._args: unknown[]) {
    throw new Error("Run `npm run compile:counter` to generate contract bindings.");
  }
}
